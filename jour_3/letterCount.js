var search = 'o';
var str = 'hello apple pie';

function letterCount(str, search) {
    var sentence = 0;
    if (str.search('o') === 1) {
      sentence = search;
    } else {
      sentence = "absent";
    }
    return search;
}
    console.log(letterCount(str + sentence + search));

    // à terminer //
   // revoir les conditions //